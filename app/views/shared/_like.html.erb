<% if current_user.is_like?(object) %>
  <%= link_to "unlike", unlike_restaurant_path(object), method: :post, class: "btn btn-info" %>
<% else %>
  <%= link_to "like", like_restaurant_path(object), method: :post, class: "btn btn-primary" %>
<% end %>